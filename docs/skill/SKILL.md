---
name: pain-point-discovery
description: >
  Systematic discovery and validation of user pain points and product opportunities.
  Use when user wants to research market demand, find what to build, validate ideas,
  discover user complaints, or mine communities for unmet needs.
  Triggers: 用户需求, 痛点, 市场调研, 需求发现, pain point, user needs, market research,
  demand discovery, idea validation, what to build, product opportunity, Reddit research,
  social listening, 找需求, 挖痛点, 竞品分析, user complaints, 插件, 变现, 开发什么,
  build what, monetize, validate idea, 竞品差评, G2, Capterra, indie hacker, side project
---

# Pain Point Discovery — 发现痛点 → 验证 → 变现

## 目标闭环

```
发现痛点 → 评估可构建性 → 验证有人付钱 → AI快速开发 → 售卖变现
```

本 skill 覆盖前三步。发现 ≠ 验证，验证 ≠ 变现。每步有明确的信号标准。

---

## 两种模式

| | Mode 1: WebSearch 快速探测 | Mode 2: API/CLI 深度挖掘 |
|---|---|---|
| **定位** | Leads generation — 广撒网找方向 | 数据驱动 — 量化验证 |
| **工具** | WebSearch（Claude 内置） | pain-miner CLI + 各平台 API |
| **耗时** | 10-30 min | 1-4 hours |
| **输出** | 待验证的痛点候选列表 | 带数据支撑的排序报告 |
| **能做** | 发现抱怨模式、竞品差评、功能缺口 | engagement 量化、趋势分析、跨平台交叉验证 |
| **不能做** | ❌ engagement 验证（看不到投票数）<br>❌ 置信度判断 | ❌ 替代真实用户访谈 |

> **铁律**: Mode 1 的所有发现标记为 `🔍 探测阶段-待验证`。单平台发现不可标为 High Confidence。

---

## 7 步流程

### Step 1: 定义范围

**目标**: 明确调研边界，避免大海捞针。

引导用户回答：
1. **领域**: 你熟悉或感兴趣的技术/行业？
2. **子领域拆解**: 把大领域拆成 3-5 个可搜索的子领域
   - ❌ "开发者工具" → 太宽
   - ✅ "VS Code 插件生态" / "CI/CD 配置管理" / "API 文档生成"
3. **目标用户**: 谁会为此付钱？（开发者 / 设计师 / 运营 / 小企业主）
4. **约束**: 你能投入多少时间开发？（1周 / 1月 / 3月+）

**输出**: 3-5 个子领域 × 目标用户矩阵

---

### Step 2: 竞品映射（新增）

**目标**: 先看已有解决方案，找空位而非重复造轮子。

对每个子领域执行：

```
搜索查询:
- "[子领域] tools 2025 2026"
- "[子领域] alternatives comparison"
- "best [子领域] software pricing"
- site:g2.com "[子领域]" OR site:capterra.com "[子领域]"
```

**记录竞品信息**:

| 竞品 | 定价 | 核心差评 | 缺失功能 | 用户量级 |
|------|------|----------|----------|----------|
| Tool A | $29/mo | 配置复杂 | 无API | ~5K |
| Tool B | 免费 | 功能少 | 缺X/Y | ~50K |

**关键判断**:
- 无竞品 → 可能没需求（危险信号），需要 Step 3 额外验证
- 有竞品但贵 → 做便宜版 / 做细分场景版
- 有竞品但差评多 → 最佳机会，差评 = 产品需求文档
- 有竞品且好评多 → 避开，除非你有 10x 优势

---

### Step 3: 多平台搜索

**目标**: 从用户真实抱怨中提取痛点信号。

#### 搜索策略矩阵

| 平台 | 搜索方法 | 最佳信号 |
|------|----------|----------|
| **Reddit** | `site:reddit.com "[产品]" OR "[领域]" "frustrated" OR "alternative" OR "wish" OR "hate"` | 长帖+多回复=真痛点 |
| **Hacker News** | `site:news.ycombinator.com "[领域]"` 或 Algolia API | 技术用户深度讨论 |
| **X/Twitter** | `"[具体产品]" ("broken" OR "not working" OR "please add" OR "why can't")` | @ 产品官方的吐槽 |
| **G2** | `site:g2.com "[产品]" cons OR "missing feature" OR "wish it had"` | 结构化差评（Cons栏）= 金矿 |
| **Capterra** | `site:capterra.com "[产品]" cons OR "disappointed"` | 同上，中小企业用户为主 |
| **App Store** | `site:apps.apple.com "[产品]" reviews` | 1-2 星差评 |
| **Product Hunt** | `site:producthunt.com "[领域]"` | 新产品 launch 评论中的需求 |
| **Chrome Web Store** | `site:chromewebstore.google.com "[领域]"` | 插件差评 = 插件机会 |

> **⚠️ `site:` 搜索 fallback**: WebSearch 的 `site:reddit.com` 经常返回 0 结果（搜索引擎限制）。遇到时改用不带 `site:` 的泛搜：`reddit [产品] frustrating alternative [年份]`。同理适用于其他 `site:` 查询。HN Algolia API 是最稳定可靠的数据源，优先使用。

#### 高价值搜索模板

```
# 功能缺口型
"[产品] missing feature" OR "[产品] feature request" OR "[产品] roadmap"

# 替代品寻找型
"[产品] alternative" OR "looking for something like [产品] but"
"switching from [产品]" OR "migrating from [产品]"

# 直接痛点型
"[领域] pain point" OR "[领域] frustrating" OR "[领域] waste of time"
"I wish there was a tool that" "[领域]"

# 付费意愿型（最高价值）
"[领域] worth paying" OR "shut up and take my money" "[领域]"
"would pay for" "[领域/功能]"
```

#### Reddit .json 端点提示（Mode 2）

Reddit 搜索结果页 URL 末尾加 `.json` 可获取结构化数据（无需 API key）：
```
https://www.reddit.com/r/SUBREDDIT/search.json?q=QUERY&sort=relevance&t=year&limit=100
```
详见 `references/tools-and-apis.md`。

#### pain-miner CLI（Mode 2 首选）

```bash
cd ~/code/pain-miner
# HN 搜索（已可用）
npx ts-node src/cli.ts search --platform hackernews --query "[关键词]" --days 30

# Product Hunt（已可用）
npx ts-node src/cli.ts search --platform producthunt --query "[关键词]" --days 30

# Grok 补充 X/Twitter（pain-miner X 模块代码就绪但需 API）
# → 直接在 Grok 界面搜索 X 帖子作为补充
```

---

### Step 4: 分类与评估

**目标**: 把零散抱怨聚合为可评估的痛点条目。

对每个发现的痛点，填写：

```markdown
## 痛点 #N: [一句话描述]

**问题本质**: [用户在什么场景下遇到什么障碍]
**发现来源**: [平台 + 帖子链接/搜索词]
**信号强度**: [多个平台重复出现 / 单平台单帖 / 仅推测]

### 评估维度
- **Frequency（频率）**: [每天遇到 | 每周 | 偶尔 | 一次性]
- **Intensity（强度）**: [阻断工作流 | 烦人但能绕过 | 轻微不便]
- **Willingness to Pay（付费意愿）**: [已有付费替代品 | 有人说愿意付费 | 未知]

### 构建评估（新增）
- **Buildability**: [插件/脚本(1周) | 小工具(1月) | 完整SaaS(3月+)]
- **Monetization Model**: [一次付费 | 订阅 | Freemium | 开源+付费支持]
- **Existing Solutions**: [无 | 有但贵($X/mo) | 有但烂(差评原因) | 有但复杂]
- **AI Leverage**: [高(AI是核心能力) | 中(AI加速开发) | 低(纯工程)]
```

**分类规则**:
- 3+ 个独立来源提到同一痛点 → 合并为一个条目，信号强度升级
- 只有 1 个来源 → 保留但标记 `⚠️ 单源-待交叉验证`
- 来源全是同一个人 → 标记 `🚨 可能是个人偏好，非普遍痛点`

---

### Step 5: 反偏差检验（新增）

**目标**: 主动寻找反面证据，避免确认偏差。

> 最危险的错误：只搜抱怨不搜解决方案，以为发现了蓝海。

对 Top 5 痛点候选，执行以下检查：

#### 5a. 搜索反面证据
```
"[痛点关键词] solved" OR "[痛点] not a problem anymore"
"[产品] fixed [功能]" OR "[产品] update [功能]"
"[产品] workaround" OR "[痛点] easy fix"
```
- 如果大量 "已解决" 结果 → 降级或剔除

#### 5b. 检查 Vocal Minority
- 抱怨帖是否来自同一批用户？（检查用户名）
- 抱怨数 vs 该产品总用户量 = 占比多少？
- 5 个人大声抱怨 ≠ 5000 人有这个问题

#### 5c. 抱怨者 = 付费者？
- 开发者在 HN 吐槽免费工具 ≠ 他们会为替代品付钱
- 检查：抱怨者是免费用户还是付费用户？
- 检查：抱怨的功能在竞品的什么价格档位？

#### 5d. 时效性检查
- 抱怨帖子的日期？（>12 个月前的痛点可能已被解决）
- 该产品最近有无重大更新？（changelog 检查）

**输出**: 每个候选痛点附加一个 `反面证据摘要`，标注为 `✅ 未找到解决方案` 或 `⚠️ 存在部分解决方案` 或 `❌ 已被充分解决`。

---

### Step 6: 排序与输出

**目标**: 生成可行动的优先级排序。

#### 评分公式

```
总分 = 信号强度(0-3) + 频率(0-3) + 付费意愿(0-3) + 构建可行性(0-3) + AI杠杆(0-2) - 竞品饱和度(0-2)
```

| 维度 | 0 | 1 | 2 | 3 |
|------|---|---|---|---|
| 信号强度 | 推测 | 单平台单帖 | 单平台多帖 | 多平台重复 |
| 频率 | 一次性 | 偶尔 | 每周 | 每天 |
| 付费意愿 | 未知 | 有免费替代 | 有付费替代(贵) | 明确说愿付费 |
| 构建可行性 | 3月+ | 1月 | 1-2周 | <1周 |
| AI杠杆 | — | 低 | 中(加速开发) | — |
| 竞品饱和度 | — | 竞品少/差 | 竞品多但有空位 | — |

> 注意：AI杠杆最高2分，竞品饱和度是减分项。

#### 输出模板

```markdown
# 痛点调研报告: [领域]
日期: YYYY-MM-DD
模式: Mode 1 (WebSearch) / Mode 2 (API+CLI)

## 执行摘要
- 调研范围: [子领域列表]
- 搜索平台: [实际搜索的平台]
- 发现痛点数: X 个（其中 Y 个通过反偏差检验）

## Top 3 机会（按总分排序）

### 🥇 #1: [痛点名称] — 总分: X/14
- **一句话**: [用户在__场景下需要__但现有方案__]
- **信号**: [来源平台 + 数量]
- **竞品**: [现有方案及其缺陷]
- **构建方案**: [Buildability] / [AI Leverage]
- **变现**: [模式] / [参考定价: $X based on 竞品]
- **反面证据**: [✅/⚠️/❌ + 摘要]
- **置信度**: [🔍 探测阶段 | 📊 数据支撑 | ✅ 已验证]
- **下一步**: → Step 7 的具体验证行动

### 🥈 #2: ...
### 🥉 #3: ...

## 完整痛点列表
[所有分类后的痛点条目]

## 搜索日志
[实际执行的搜索查询 + 每个查询的发现数]
```

#### 置信度标准（严格定义）

| 置信度 | 条件 |
|--------|------|
| 🔍 探测阶段 | Mode 1 发现 OR 单平台发现 OR 无 engagement 数据 |
| 📊 数据支撑 | Mode 2 数据 + 多平台交叉验证 + 通过反偏差检验 |
| ✅ 已验证 | 完成 Step 7 验证（关键词搜索量 + 付费意愿信号） |

---

### Step 7: 快速验证（新增）

**目标**: 用最小成本验证痛点是否值得开发。

> 不要跳过验证直接开发。即使是 1 周的项目，1 天验证也值得。

#### L1: 桌面研究（10-30 min）

最低成本，所有痛点都应过一遍：

```
1. Google Trends: 搜索 "[痛点关键词]" 趋势
   - 上升趋势 → 加分
   - 平稳 → 中性
   - 下降 → 减分（可能正在被解决）

2. 竞品定价锚点:
   - 找到最贵的竞品 → 你的定价天花板
   - 找到最便宜的竞品 → 你的定价地板
   - 无竞品 → 参考相似领域

3. 关键词搜索量（Google Keyword Planner / Ubersuggest）:
   - "[痛点] tool" / "[痛点] software" / "[痛点] solution"
   - 月搜索量 > 1000 → 有市场
   - 月搜索量 100-1000 → 细分市场
   - 月搜索量 < 100 → 可能太小众
```

#### L2: Fake Door 测试（1-2 hours）

对 L1 通过的 Top 1-2 个痛点：

```
1. 写一个 landing page（用 v0.dev / Cursor 5 分钟生成）
   - 标题: [解决X痛点的一句话]
   - 3 个 feature bullet points
   - CTA: "Join waitlist" / "Get early access"
   - 部署到 Vercel

2. 投放测试:
   - 发到相关 Reddit/HN 帖子下（以分享工具的方式，不要硬广）
   - 发到相关 Discord/Slack 社群
   - 记录: 点击率、注册数

3. 判断标准:
   - 24h 内 > 10 个注册 → 强信号，值得开发
   - 24h 内 3-10 个注册 → 中等信号，考虑 MVP
   - 24h 内 < 3 个注册 → 弱信号，换方向或改定位
```

#### L3: Pre-sell 验证（如果 L2 结果好）

```
120 字痛点邮件模板:

Subject: Quick question about [痛点]

Hi [name],

I noticed you mentioned [具体痛点] in [帖子链接].

I'm building a [一句话描述工具] that [解决方式].
Early access price is $X (正式版 $Y).

Would this solve your problem? Reply "interested" and I'll add you to the list.

[你的名字]
```

#### 验证决策矩阵

| 痛点类型 | 推荐验证方式 | 原因 |
|----------|-------------|------|
| 开发者工具/插件 | L1 + L2（HN/Reddit 发帖） | 开发者社区反馈快 |
| B2B SaaS | L1 + L3（直接联系抱怨者） | 决策链长，需要直接对话 |
| 浏览器插件 | L1 + L2（Chrome Web Store 对比） | 安装数公开，容易估算市场 |
| 一次性工具 | L1 即可（看搜索量） | 低风险，快速开发快速验证 |

---

## Honesty Rules（诚实规则）

这些规则不可违反，即使用户催促也不行：

1. **Mode 1 = 探测器**：WebSearch 看不到 engagement 数据（点赞数、评论数），不编造数据，不基于想象做量化判断。所有 Mode 1 发现标记为 `🔍 探测阶段-待验证`。

2. **单源不可高置信**：只在一个平台发现的痛点，无论描述多生动，最高只能是 `📊 数据支撑`（如果有 engagement 数据），通常只是 `🔍 探测阶段`。

3. **不伪造 engagement**：如果看不到具体投票/评论数，写 `[WebSearch 模式无法获取]`，不要写 "high engagement" 或 "many upvotes"。

4. **竞品信息标注来源**：定价、用户量等数据标注 `[来源: G2/官网/推测]`。推测要明确标注。

5. **区分抱怨和需求**：有人抱怨 ≠ 有人愿意付钱。在报告中明确区分 `用户抱怨` 和 `付费需求信号`。

6. **承认盲区**：如果某个子领域搜索结果太少，明确说"数据不足以判断"，不要填充推测。

---

## 参考资源

- 工具与 API 详情 → `references/tools-and-apis.md`
- pain-miner CLI → `~/code/pain-miner`（GitHub: sunny-kobe/pain-miner）
- 痛点研究方法论 → 用户 memory 中的 `pain-point-research-deep-dive.md`
